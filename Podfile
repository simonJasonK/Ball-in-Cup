# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Ball in Cup' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Google-Mobile-Ads-SDK', '~> 7.8'
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'

  # Pods for Ball in Cup

  target 'Ball in CupTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Ball in CupUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
